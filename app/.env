#this helps us not to manually set up our environment variables


#export the environment variables having defined them
export FLASK_APP=app
#pip install python-dotenv (command helps to install a library that helps us to work with the environment varia)